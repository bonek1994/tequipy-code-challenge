apiVersion: v1
kind: Service
metadata:
  name: tequipy-app
  labels:
    app: tequipy-app
spec:
  type: ClusterIP
  selector:
    app: tequipy-app
  ports:
    - port: 8080
      targetPort: 8080
      protocol: TCP
