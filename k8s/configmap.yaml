apiVersion: v1
kind: ConfigMap
metadata:
  name: tequipy-config
data:
  APP_PORT: "8080"
  DB_HOST: "postgres-service"
  DB_PORT: "5432"
  DB_NAME: "tequipy"
  RABBITMQ_HOST: "rabbitmq-service"
  RABBITMQ_PORT: "5672"
